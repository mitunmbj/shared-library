@Library('mitun-library@master') _
 
pipeline {
	stages {
           stage('Git Checkout') {
            steps {
            gitCheckout(
                branch: "master",
                url: "https://github.com/mitunmbj/shared-library.git"
                 )
            }
	}

	}}
